const FooterComponent = () => {

    return (
        <footer
            className={`
                    bg-blueDark text-black
                    dark:bg-black dark:text-white
                    p-[0.2rem] select-none flex flex-shrink
                    `}
        >
        </footer>
    )
}

export default FooterComponent